# TEMCO Product Studio v1.4 — 项目说明文档

> 版本：v1.4（2026-08）
> 面向：使用人员（现场采集、电脑审核）

---

## 1. 系统简介

TEMCO Product Studio 是西班牙电商 TEMCO 的商品管理系统，与 PrestaShop 网站（www.temco.es）实时对接。v1.4 加入**手机采集模块**：现场人员用手机扫条码/搜索产品、拍照上传、标注颜色/库存/适用手机型号，电脑端审核后一键同步图片与变体到网站。

**核心价值**：
- 📱 手机现场采集（LAN 直连，无需外网）
- 🖼 AI 精修图工作流（原始照片只留本地，网站只上精修图）
- 🧬 网站变体实时管理（读取/创建/修改/同步）
- 📊 手机壳点货统计（品牌 → 型号 → 颜色）

---

## 2. 功能总览

### 电脑端（浏览器访问 `http://localhost:5173` 或局域网 IP）

| 功能 | 说明 |
|---|---|
| 📱 手机采集 | 查看局域网访问 IP / PIN / 二维码，管理会话 |
| 🧾 采集审核 | 审核手机提交的任务：照片、颜色、库存、手机型号、处理图、变体 |
| 🛍 产品管理 | 产品列表、属性编辑、8 槽位图片、同步图片到网站 |
| 🗂 分类管理 | 分类树 |
| 📷 产品图片 | 产品图片管理（扫描图/上传批次） |

### 手机端（浏览器访问 `http://192.168.1.27:5173/mobile-capture`）

| 功能 | 说明 |
|---|---|
| PIN 登录 + 采集会话 | 操作员/设备/区域，会话码 CAP-xxx |
| 扫码 / 搜索产品 | 拍照扫码或输入型号/Reference/EAN；**实时读取网站数据**（价格/图片数/库存/变体/启用状态） |
| 新增产品 | 本地/网站都没有时，直接新增（名称/序列号/条码/价格/颜色） |
| 拍照上传 | 拍照/相册，标注照片角色（正面/背面/细节）+ 颜色 |
| 颜色 / 库存 | 产品颜色标注、按颜色录入库存 |
| 📱 手机型号点货 | 勾选适用手机型号（三星/小米/iPhone 分组），每个型号可选颜色（红/黄/蓝/绿…） |
| 备注 / 语音 | 文字备注 + 语音备注 |
| 提交 | 提交任务到电脑端审核 |
| 已卖完标记 | 巡视发现断货时一键标记「🚫 已卖完」 |

---

## 3. 快速开始

```bash
# 后端（3001）
cd server && npm run dev
# 前端（5173，host: true 局域网可访问）
cd client && npm run dev
```

- 电脑端：`http://localhost:5173` → 顶栏「📱 手机采集」「🧾 采集审核」
- 手机端：连同一 Wi-Fi → 浏览器访问电脑端「手机采集」页面显示的 IP + `/mobile-capture`
- PIN：在电脑端「手机采集」设置（如已设置则手机端需输入）

---

## 4. 手机端使用流程

### 4.1 登录与会话
1. 打开 `/mobile-capture` → 填操作员/设备名（有 PIN 则填）→ 开始采集
2. 「＋ 创建新会话」（或继续已有会话）

### 4.2 找产品
- **扫码**：拍产品条码 → 自动识别（EAN/Code128/QR）→ 匹配产品
- **搜索**：输入型号/Reference/序列号/EAN → 点「搜索」（**不会自动搜**）
- 结果含义：🟢 网站已启用 · 🔴 网站未启用/下架 · ⚪ 未同步网站
- 产品详情顶部显示「🌐 网站实时数据」：图片数、库存（真实 stock_availables）、变体（颜色×库存）、价格
- 列表进入详情后可按「← 返回搜索结果」回到列表，无需重新搜索
- 都找不到 → 「➕ 未找到，新增产品」填信息创建

### 4.3 采集
1. 📷 拍照/🖼 相册上传（可多张）→ 每张标注角色 + 颜色
2. 🎨 产品颜色：勾选（与网站变体颜色一致）
3. 📦 库存：按颜色填数量（精确/大约/充足/未盘点）
4. 📱 **手机型号（点货统计）**：品牌分组（iPhone/Samsung/Xiaomi/OPPO/vivo/realme…）→ 勾选型号 → 选颜色（红/黄/蓝/绿…）；大品牌点「展开全部 N 个」
5. 📝 备注 / 🎤 语音
6. 底部：「保存草稿」「提交任务」「保存并处理下一个」

> 中断恢复：采集一半退出后回来，继续任务 → 直接点「提交任务」会自动重新打开（不再报"submitted 无法提交"）。

### 4.4 已卖完标记
搜索出产品后，产品摘要区点「🚫 标记已卖完」→ 电脑端该产品卡片显示红字「🚫 已卖完」。

---

## 5. 电脑端审核流程

1. 打开「🧾 采集审核」→ 统计卡（待审核/待图片/待颜色…）+ 任务列表（15 秒自动刷新）
2. 卡片显示：产品缩略图、状态、颜色、**手机型号（点货统计）**、已卖完红字、操作员、时间
3. 点卡片进详情：
   - **原始照片**：查看/下载 → 用 AI 精修 → 上传为「处理后照片」（原始照片不会上网站）
   - 图片审核：通过/拒绝/改角色/改颜色
   - 🎨 颜色审核、📦 库存审核
   - 📱 手机型号（点货统计）标签显示
   - 🧬 网站变体：读取网站现有变体 → 增删改 → **同步变体到网站**（按采集颜色+库存一键）
4. 任务操作：通过 / 打回 / 就绪
5. 勾选多卡片可**批量删除**

### 同步到网站
- **图片**：「同步产品图片到网站」→ 只上传「处理后照片」（AI 精修图）；未上传处理图时会明确提示
- **变体**：「同步变体到网站」→ 采集标注的颜色+库存 → 网站已有该颜色变体则更新库存，没有则新建

---

## 6. 手机型号点货统计（重点）

场景：手机壳型号极多，按型号分类管理（不用网站变体）。

- 型号目录 = 预置常用 + **网站自动同步**（Modelo 属性组 + 网站分类树：`Accesorios para SAMSUNG` 85 个、`Accesorios iPhone` 39 个、XIAOMI/REDMI/POCO/OPPO/VIVO/REALME 等）
- 手机端：品牌分组勾选型号 → 每个型号可点颜色（红/橙/黄/绿/蓝/紫/粉/黑/白/灰/透明/其他）
- 保存后电脑端卡片/详情显示：**📱 iPhone 17（红、蓝）、Galaxy A15（黑）…**
- **仅做统计，不同步网站**

---

## 7. 数据存储

| 内容 | 位置 |
|---|---|
| SQLite 数据库 | `server/data/temco.db` |
| 手机采集照片 | `server/data/mobile-captures/`（按任务分目录） |
| 处理图/产品图 | 数据库引用 + 上传目录 |

> ⚠️ 不要手动删除 `data/mobile-captures` 目录（会丢失照片记录），用系统内删除操作。

---

## 8. 常见问题

| 问题 | 处理 |
|---|---|
| 手机访问白屏 | 确认手机与电脑同一 Wi-Fi；访问电脑「手机采集」页显示的 IP |
| 提示"未登录或会话已过期" | 重新输入 PIN 登录；后端重启后 token 仍有效（持久化） |
| 提交报"该任务当前状态为 submitted" | 已修复：提交会自动重新打开，直接再点提交即可 |
| 手机型号没全部出现 | 点品牌右侧「展开全部 N 个」；目录来自网站分类树，自动同步 |
| 颜色选择器混入型号 | 已修复：只显示网站 Color 组颜色 |
| 库存显示 0 | 已修复：改读网站 stock_availables 真实库存 |
| 图片同步失败"没有处理后照片" | 先上传 AI 精修处理图（原始照片不上网站） |
| 变体同步报权限 | 在 PrestaShop 后台 API Key 勾选 combinations / product_option_values / stock_availables |
| 已卖完标记 | 手机端产品摘要「🚫 标记已卖完」，电脑端卡片红字显示 |

---

## 9. 后续规划

- 第二阶段：AI 图片自动处理、变体正式批量同步
- 第三阶段：PWA 离线采集
- 手机型号目录后台维护界面

---

# 附：v1.5 仓库快速盘点（使用说明）

> 面向仓库人员。产品款式 → 手机型号 → 颜色 → 数量 → 盘点批次，不依赖固定货位。

## 手机端（`/mobile-inventory`）

1. **盘点首页**：继续进行中的批次，或「＋ 新建盘点」（填名称，自动生成 `INV-20260807-001`）
2. **选择产品**：输入 Reference / EAN 搜索 → 点击产品开始盘点
3. **选择品牌**：iPhone / Samsung / Xiaomi / OPPO…（显示已盘 N / 总数）
4. **连续型号盘点**（核心）：
   - 一次只盘一个型号（如 iPhone 15 Pro），顶部进度 3/39
   - 每个颜色一行：选颜色（黑/白/灰/红/橙/黄/绿/蓝/紫/粉/棕/透明/其他）→ `[-5][-1]` 数量 `[1][+5]` 或直接点数字输入
   - 数量填 0 = 🔴 无货；「精确 / 大约」切换
   - **☑ 自动使用上一型号颜色**（默认开启，换型号不用重选颜色）
   - 改数量自动保存（顶部 ✓ 已保存）；「跳过」= 本型号不盘（≠ 无货）
   - 「保存并下一个 →」直接进入下一型号，全程不回列表
5. **汇总矩阵**：型号×颜色 表格 + 统计（型号/颜色记录/总数/🔴无货/🟠少量）+ ⚠ 与网站差异大项
6. **✓ 完成盘点**：批次状态 completed，历史保留

> 中途退出：重进自动提示「继续上次盘点」（进度记忆在手机本地）。

## 电脑端（`/inventory`，顶栏「📦 仓库盘点」）

- **当前盘点**：批次卡（操作员/开始时间/统计）+ 产品卡 → 点击查看**汇总矩阵**弹窗
- **历史记录**：所有已完成的批次（数量/型号/缺货数）
- **库存差异**：实盘 vs 网站快照，大差异（绝对值 > 3）红色标出

## 与 v1.4 商品采集的关系

- 商品采集（`/mobile-capture`）：产品上架/图片/变体/网站资料
- 仓库盘点（`/mobile-inventory`）：仓库里有什么、什么型号、什么颜色、还有多少
- 两套数据完全分离，互不影响；盘点数据**只对比不同步网站**（防止误覆盖）
